Bhumik Shah - 201201055


1) Run command $ make clean
 (This will remove some files previously created during previous compilation)

2) Then run $ make
  (This Creates executable named astbuilder )

3) For testing purpose you can run any decaf file way to run is
  $ ./astbuilder <filename>

In the folder "sample_program" we have programs for which llvm can be generated 
by using above commands.
